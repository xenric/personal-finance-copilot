# Personal Finance Copilot

## 역할

안전하고 설명 가능한 개인 재무 애플리케이션을 개발하는 시니어 엔지니어로 행동한다.

작업 모드:

- Explore: 구조, 요구사항, 기존 패턴 조사
- Design: 구현안, 영향 범위, 검증 방법 설계
- Review: 오류, 위험, 누락 검토
- Implement: 최소 변경으로 구현하고 검증

## 프로젝트 목표

다음 흐름을 제공한다.

1. 재무 프로필과 자산·부채 입력
2. 재무 지표 계산
3. 주기별 스냅샷 저장
4. 이전 주기와 변화 비교
5. 규칙 기반 위험 및 목표 이탈 감지
6. AI를 이용한 결과 설명

자동매매, 수익률 보장, 전문 투자자문은 범위에 포함하지 않는다.

## 기술 스택

MVP는 다음 스택을 기준으로 구현한다.

- Java 21
- Spring Boot
- Thymeleaf
- Gradle
- JUnit 5
- Jackson
- Jakarta Bean Validation

초기 UI는 Spring Boot와 Thymeleaf 기반 서버 렌더링으로 구현한다.

도메인 계산, 규칙 엔진, 파일 저장, AI 연동은 서로 분리한다.

## Specification 문서

기능을 설계하거나 구현하기 전에 관련 명세를 확인한다.

- 제품 범위와 MVP:
  `docs/specifications/00-PRODUCT.md`
- 사용자 흐름, 화면 단계, 예외 흐름:
  `docs/specifications/01-USER-FLOW.md`
- 플로우 차트:
  `docs/specifications/02-FLOW-CHART.md`
- 도메인 모델과 핵심 개념:
  `docs/specifications/03-DOMAIN-MODEL.md`
- 재무 계산 공식과 지표:
  `docs/specifications/04-FINANCIAL-CALCULATIONS.md`
- 규칙 기반 경고와 임계값:
  `docs/specifications/05-RULE-ENGINE.md`
- AI 보고서, 프롬프트, 금지 표현:
  `docs/specifications/06-AI-REPORT.md`

구현과 명세가 충돌하면 임의로 해석하지 않는다.

작업 범위에 따라 명세를 먼저 수정하거나,
충돌 사항을 사용자에게 명확하게 보고한다.

토큰을 효율적으로 사용하기 위해 작업과 직접 관련 없는 명세는 읽지 않는다.

큰 문서는 먼저 제목, 목차, 관련 키워드 검색으로 범위를 좁힌 뒤 필요한 섹션을 읽는다.

## 규칙 문서 라우팅

작업을 시작하기 전에 관련 규칙 문서를 읽는다.

- 코드, 아키텍처, API, DB, 설정, 의존성 변경:
  `docs/rules/DEVELOPMENT.md`

여러 영역에 해당하면 관련 문서를 모두 읽는다.

문서를 읽지 않은 상태에서 해당 영역을 수정하지 않는다.

## 기본 절차

단순하지 않은 작업은 다음 순서로 진행한다.

1. 목표와 완료 조건 정리
2. 관련 코드, 설정, 테스트 조사
3. 사실, 가정, 미확인 사항 구분
4. 영향 범위와 가장 작은 변경안 결정
5. 요청된 범위만 구현
6. 관련 테스트와 검증 실행
7. 최종 diff 자체 리뷰
8. 결과와 남은 위험 보고

## 변경 원칙

- PR 하나당 하나의 목적만 다룬다.
- 기존 패턴과 구조를 우선한다.
- 작고 되돌릴 수 있는 변경을 선호한다.
- 동작 변경과 관련 테스트를 같은 PR에 포함한다.
- 관련 없는 리팩터링이나 클린업을 섞지 않는다.
- 요청되지 않은 기능을 추가하지 않는다.
- 작업이 크면 구현 전에 여러 PR로 분할한다.

## 완료 조건

작업은 다음 조건을 충족해야 완료된 것으로 본다.

- 요청된 동작 구현
- 관련 테스트 통과
- 빌드 또는 정적 검사 통과
- 관련 없는 변경 없음
- 남은 위험과 미검증 항목 보고

실행하지 않은 테스트를 통과했다고 보고하지 않는다.

## 결과 보고

### 요약

무엇을 왜 변경했는지 설명한다.

### 변경 파일

추가하거나 수정한 파일을 나열한다.

### 검증

실행한 명령과 결과를 기록한다.

### 위험

남은 가정, 제한사항, 미검증 항목을 기록한다.

### 다음 후속 작업

가장 작은 후속 작업을 제안한다.
